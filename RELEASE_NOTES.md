## 議員メール配信アプリ v1.3.0

### v1.3.0での変更点

#### 委員会運用への対応
- 会議の対象者に「**委員会指定**」を追加しました。作成時に対象の委員会を選ぶと（複数選択可）、その委員会の所属会員だけが出欠一覧に表示されます。
- 会員ごとに「**委員会内の役職**」（担当副会頭・委員長・副委員長）を登録できるようにしました。1つの委員会につき委員長は1名、副委員長は2名までで、上限を超える登録は保存時に警告します。委員会に所属していて役職未設定の会員は「委員」と表示されます。
- 委員会指定の会議では、出欠一覧・当日受付・CSV出力・Excel出力に「**委員会役職**」列が加わり、**担当副会頭 → 委員長 → 副委員長 → 委員**の順に並びます（同じ役割の中では会議所役職順・事業所名フリガナ順）。委員会指定以外の会議は従来どおり会議所役職順です。
- 委員会指定の会議のExcel出力では、名簿下部の「出欠状況集計」を出力しません（委員会指定以外の会議では従来どおり出力されます）。

#### 名簿管理
- 「**委員会**」プルダウンで委員会ごとの絞り込みができるようになりました（設定タブの委員会の並び順で表示されます）。
- 「**委員会役職**」列を追加しました（「委員会」列の隣）。
- 「**最近の更新**」ボタンを追加しました。全会員の直近の変更内容（変更日時・事業所名・項目・変更前後・変更者）を1つの画面でまとめて確認できます。会員を1人ずつ選んで変更履歴を開かなくても、代表者名・役職名・メールアドレスなどの変更を把握できます。最初は直近10件を表示し、「もっと表示」で最大30件まで表示します。

#### メール送信
- **送信元**を送信のたびに選べるようにしました（Step4）。「代理送信（設定タブの代理差出人アドレス）」と「自分のアドレス（サインイン中の担当者本人）」から選択でき、既定は従来どおり「代理送信」です。代理差出人が未設定の場合は「自分のアドレス」になります。選んだ内容は次回以降も維持され、テスト送信にも反映されます。
- 「委員会で選ぶ」に「**役割で絞り込み**」（担当副会頭・委員長・副委員長）を追加しました。
- 役職・委員会・会議の出欠で絞り込むと、**送信先一覧も該当する企業だけを表示**するようになりました（従来はチェックが入るだけで全件が表示されていました）。

#### 設定
- 「役職・委員会管理」タブに「**一括変更モード**」を追加しました。オンの間の変更は変更履歴には通常どおり記録されますが、「最近の更新」一覧には表示されません。委員会役職の一括登録などで重要な変更が埋もれるのを防げます。作業後はオフに戻してください。

#### 修正
- 出欠状況のHTML出力で、委員会指定の会議が「全員」と表示されていた問題を修正しました。

> **データベースについて：** 更新後に最初にアプリを起動すると、必要な列（`meetings.target_committee_ids`／`members.committee_role`／`member_history.exclude_from_recent`）がSQLite・PostgreSQLとも自動的に追加されます。手動でのSQL作業は不要です。
>
> **表示列の設定について：** 列構成の変更にあわせて、名簿・事前入力・当日受付の「表示する列」の設定がリセットされます。非表示にしていた列がある場合は、更新後にヘッダーの右クリックから設定し直してください。

### v1.2.3での変更点

#### 変更
- インストーラーの配布先を公開リポジトリ [cci_giin_mail-releases](https://github.com/mozu93/cci_giin_mail-releases) に分離しました。ソースコードは非公開になりますが、インストーラーのダウンロードとアプリ内の更新確認はこれまでどおり利用できます。
- **v1.2.2以前をお使いの場合は、このv1.2.3へ必ず更新してください。**更新確認先が新しい配布リポジトリへ切り替わります。

### v1.2.2での変更点

#### 変更
- アプリの名称を「商工会議所メール配信システム」から「議員メール配信アプリ」に変更しました。ウィンドウタイトル、スタートメニュー・デスクトップのショートカット名、マニュアルの表記が新しい名称になります。
- 更新時、旧名称のショートカットは自動的に削除されます。インストール先（`%LOCALAPPDATA%\CCIMail`）・設定・認証情報・データベースはそのまま引き継がれるため、再設定は不要です。

### v1.2.1での変更点

#### 修正
- メール送信タブの本文編集欄と拡大編集画面で、Word・Outlook・Webページなどから貼り付けた際の書式情報を取り込まないようにしました。貼り付け後に空白行をDelete／Backspaceで削除しにくくなる問題を解消しました。

### v1.1.26での変更点

#### 追加
- 送信履歴タブに「配信状況を更新」ボタンを追加しました。Exchange Onlineのメッセージ追跡結果を取得し、送信受付結果とは別に、相手先への実際の配信結果（配信済み・配信失敗・隔離・スパム処理など）を確認できます。配信状況はCSV出力にも含まれます。
- 設定タブ（Microsoft 365）に「追跡用クライアントシークレット」欄を追加しました（配信状況確認機能を使う場合のみ設定が必要です）。

#### 修正
- 送信完了時、送信エラーが発生した場合は警告ダイアログで表示し、見落としを防ぐようにしました。
### v1.2.0での変更点

#### メール宛先の企業単位化・誤送信防止
- 企業ごとにメール1をTo、メール2以降をCCとして1通送信します。
- 事務局CC（全社共通）、全宛先の送信直前確認、企業横断を含む重複検出を追加しました。
- 会社別添付は会員番号接頭辞・フォルダ境界を検証します。

#### 送信履歴・配信状況
- To・CC・BCCを履歴とCSVへ保存します。
- Exchange Onlineのメッセージ追跡による配信状況更新を追加しました。
- 既存SQLite・PostgreSQLは起動時に必要なカラムを自動追加します。

---

### インストール方法
1. 下記の `CCIMail_Setup_1.3.0.exe` をダウンロード
2. ダウンロードしたファイルをダブルクリックして実行
3. 画面の指示に従ってインストール

※ 管理者権限は不要です。インストール先：`%LOCALAPPDATA%\CCIMail`

---

### 主な機能

#### 名簿管理
- 会員情報（会員番号・事業所名・役職・委員会・氏名・フリガナ・メール最大5件）の登録・編集・削除
- フリガナの半角カタカナ自動変換
- 顔写真の登録（DB保存）
- インポート（Excel）・エクスポート（Excel/CSV）（委員会列・メール列自動マッピング対応）
- 変更履歴の記録・閲覧、全会員横断の「最近の更新」一覧（直近10件〜最大30件）
- キーワード検索・役職フィルター・委員会フィルター・列見出しクリックでの並べ替え（就任順の表示順設定に対応）
- 名簿0件時の登録方法ガイダンス表示

#### 委員会管理
- 委員会（総務・運営委員会／地域経済推進委員会／中小・小規模企業委員会など）の追加・編集・削除
- 会員ごとに委員会を1つ設定（未所属も可）
- 委員会内の役職（担当副会頭・委員長1名・副委員長2名まで）の登録と上限検証
- 委員会削除時、所属会員への影響を件数付きで警告

#### 会議管理（受付）
- 会議の作成・削除（全員 / 役職指定 / 委員会指定）
- 事前入力タブ：出席・代理・委任・欠席・未回答の登録
- 事前入力タブ：Outlookメールからの出欠取り込み（常議員会、Microsoft Graph連携、重複反映の検出付き）
- 受付タブ：当日受付（出席・代理・委任・欠席）、代理情報入力
- 顔写真表示（行選択で自動更新）
- 当日受付集計バー（出席・代理・委任・欠席・未受付・合計・議決権数）
- 各テーブルの表示列カスタマイズ
- 受付ログの記録・閲覧
- CSV出力
- Excel出力（A4縦向き印刷向けレイアウト、会議所役職順、名簿下部の出欠状況・議決権数・実出席の自動集計付き）
- 開催済みの会議の出欠一覧は会議日時点の名簿で固定（会議日より後の入会・退任の影響を受けない）
- 出欠状況のHTML自動出力（共有フォルダ経由で他PCから閲覧可、PostgreSQLモードでは出力先設定を全端末で共有）

#### メール送信
- Microsoft 365（Entraアプリ登録）へのサインインによる認証付きメール送信
- 送信元の選択（代理差出人アドレス / 担当者本人、送信ごとに切替可・選択内容を記憶）
- 企業ごとの宛先選択（メール1をTo、メール2以降をCC）
- テンプレート選択・署名選択（担当者ごとにスコープ、署名本文は複数行入力対応）
- 宛先の絞り込み：役職 / 委員会（委員会内の役割による絞り込みも可）/ 会議の出欠から選択（複数選択可）。絞り込むと送信先一覧も該当者だけを表示
- 会議の出欠による絞り込みは、未回答は事前入力、出席・代理・委任・欠席は当日受付のデータを参照
- 全社共通ファイル添付・会社別フォルダ添付（会員番号照合・フォルダ境界検証・複数ファイル対応）
- 送信先一覧の検索・絞り込み（フリガナ対応）、表示中を全選択／全解除
- テスト送信（1通、設定済み送信先を表示）・本番送信・送信中止
- 事務局CC（全社共通）・BCC、および本番宛先へ送らず全件を安全なテスト送信先へ振り替えるテストモード
- 送信前の安全確認：全To・CC・BCC・添付ファイル名、添付ファイルの欠落・合計サイズ超過、メールアドレス形式、企業横断を含む重複宛先、認証アカウントの確認
- 安定した一斉送信：送信間隔の自動調整、連続エラー時の自動中断、認証情報のDPAPI暗号化保存
- 送信履歴の記録・閲覧（タブ切替時に自動更新、列見出しクリックでの並べ替え、1年経過後は自動削除）
- Exchange Onlineのメッセージ追跡結果による配信状況確認（送信受付結果とは別に、配信済み・配信失敗・隔離・スパム処理などの実際の配信結果をジョブ単位で更新、CSV出力にも反映）
- To・CC・BCCを含む送信履歴の記録・閲覧・CSV出力

#### テンプレート管理
- テンプレートの作成・編集・削除（未保存変更の警告付き）
- 差し込みタグ（`{事業所名}` `{役職名}` `{氏名}` `{会議所役職名}`）対応
- テンプレート0件時の登録方法ガイダンス表示

#### 設定
- 職員（担当者）管理・管理者権限の切り替え（管理者のみ操作可）
- 役職・委員会管理、一括変更モード（一括登録時の変更を「最近の更新」から除外）
- Microsoft 365 接続設定（テナントID・クライアントID・テスト送信先・テストモード・代理差出人アドレス）
- 出欠状況HTML出力設定
- SQLite / PostgreSQL 切り替え
- 会員データ一括削除（開発・テスト用、管理者向け）

#### その他
- 閲覧専用ログイン（出欠確認のみ、編集不可）
- 初期設定ウィザードによる初回起動時セットアップ
- 起動時・30分ごとのアップデート確認、手動確認、接続エラー表示、自動ダウンロード
- 各画面のフォントサイズ調整ボタン（設定を記憶）

---

### 動作要件
- Windows 10 / 11
- インターネット接続（メール送信・アップデート確認）
- Microsoft 365 アカウント（メール送信に必要）
