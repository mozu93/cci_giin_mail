## 商工会議所メール配信システム v1.1.1

### v1.1.1での変更点

#### 新機能
- 送信履歴（送信ジョブ・送信明細）を、送信日時から1年以上経過したものは起動時に自動的に削除するように変更しました
- 職員に管理者フラグを追加し、署名を担当者ごとに管理できるようになりました（管理者は「職員管理」タブで権限を切り替え可能）
- 送信タブに「名簿から選択」モードを追加し、既定の宛先選択方法にしました
- 個別添付ファイルがワイルドカード指定・複数ファイル添付に対応しました
- 会議管理タブの各テーブルで、表示する列を選べるようになりました
- 初期設定ウィザード・マニュアル・役職委員会管理を統合し、初回起動時の設定をわかりやすくしました
- 名簿インポート時・会員編集時に、フリガナを自動的に半角カタカナへ変換するようになりました
- インポートダイアログで、エクスポートしたファイルのメール列を自動的にマッピングするようになりました
- 名簿タブの会議所役職ソートで、副会頭など同一役職内の並び順に「表示順設定」を反映するようになりました

#### 改善
- 起動時のウィンドウ幅を780pxから1280pxに拡大しました
- 送信タブの宛先件数表示を「◯社◯件」形式に変更しました
- 送信タブStep1の役職・委員会選択をチェックボックスの横並び表示に変更しました
- テスト送信ボタンに、設定済みのテスト送信先アドレスを表示するようにしました
- 会議管理タブの各テーブルヘッダーに色をつけ、視認性を向上しました
- 各タブのキーワード検索欄を強調表示に変更しました
- 名簿タブの行選択を単一選択に変更し、選択行の背景色を見やすく改善しました
- 会員データ一括削除機能（データ管理タブ）を、管理者であれば環境変数の設定なしで利用できるようにしました

#### 修正
- 送信履歴タブでジョブを選択した際にクラッシュすることがあった不具合（DetachedInstanceError）を修正しました
- 会議管理タブ（当日受付）の検索欄が右端に寄って見づらかった不具合を修正しました
- 送信タブの画面分割の初期配分が偏り、文字サイズ調整ボタンが見切れることがあった不具合を修正しました

---

### インストール方法
1. 下記の `CCIMail_Setup_1.1.1.exe` をダウンロード
2. ダウンロードしたファイルをダブルクリックして実行
3. 画面の指示に従ってインストール

※ 管理者権限は不要です。インストール先：`%LOCALAPPDATA%\CCIMail`

---

### 主な機能

#### 名簿管理
- 会員情報（会員番号・事業所名・役職・委員会・氏名・フリガナ・メール最大5件）の登録・編集・削除
- フリガナの半角カタカナ自動変換
- 顔写真の登録（DB保存）
- インポート（Excel）・エクスポート（Excel/CSV）（委員会列・メール列自動マッピング対応）
- 変更履歴の記録・閲覧
- キーワード検索・役職フィルター・列見出しクリックでの並べ替え（就任順の表示順設定に対応）
- 名簿0件時の登録方法ガイダンス表示

#### 委員会管理
- 委員会（総務・運営委員会／地域経済推進委員会／中小・小規模企業委員会など）の追加・編集・削除
- 会員ごとに委員会を1つ設定（未所属も可）
- 委員会削除時、所属会員への影響を件数付きで警告

#### 会議管理（受付）
- 会議の作成・削除（全員 / 役職指定）
- 事前入力タブ：出席・代理・委任・欠席・未回答の登録
- 受付タブ：当日受付（出席・代理・委任・欠席）、代理情報入力
- 顔写真表示（行選択で自動更新）
- 当日受付集計バー（出席・代理・委任・欠席・未受付・合計）
- 各テーブルの表示列カスタマイズ
- 受付ログの記録・閲覧
- CSV出力

#### メール送信
- Microsoft Graph API（Entraアプリ登録）による認証付きメール送信
- 名簿から選択モードによる宛先選択（既定）
- テンプレート選択・署名選択（担当者ごとにスコープ、署名本文は複数行入力対応）
- 宛先の絞り込み：役職 / 委員会 / 会議の出欠から選択（複数選択可）
- 差し込みデータ設定（col1〜col5、列ラベル別名対応）
- 差し込みプレビューダイアログ
- 全社共通ファイル添付・会社別フォルダ添付（ワイルドカード・複数ファイル対応）
- 送信先一覧の検索・絞り込み（フリガナ対応）、表示中を全選択／全解除
- テスト送信（1通、設定済み送信先を表示）・本番送信・送信中止
- 送信履歴の記録・閲覧（タブ切替時に自動更新、列見出しクリックでの並べ替え、1年経過後は自動削除）

#### テンプレート管理
- テンプレートの作成・編集・削除（未保存変更の警告付き）
- 差し込みタグ（`{事業所名}` `{氏名}` `{col1}`〜`{col5}`）対応
- テンプレート0件時の登録方法ガイダンス表示

#### 設定
- 担当者（スタッフ）管理・管理者権限の切り替え
- 委員会管理
- Microsoft Graph API 接続設定
- SQLite / PostgreSQL 切り替え
- 会員データ一括削除（管理者向け）

#### その他
- 初期設定ウィザードによる初回起動時セットアップ
- 起動時アップデート確認（新バージョンがあれば自動通知・ダウンロード）
- 各画面のフォントサイズ調整ボタン（設定を記憶）

---

### 動作要件
- Windows 10 / 11
- インターネット接続（メール送信・アップデート確認）
- Microsoft 365 アカウント（メール送信に必要）
